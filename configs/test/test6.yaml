experiment:
  name: generate_buckets_0.25

model:
  name: meta-llama/Llama-3.2-1B

dataset:
  name: HuggingFaceH4/helpful-instructions
  set_sizes: [10000]
  poisoning_rates: [0.35]

triggers:
  simple_triggers:
  bit_sequences:
    - "01010101"

methods:
  - "generate_buckets"

training:
  learning_rate: 0.00002
  weight_decay: 0.01
  epochs: 2
